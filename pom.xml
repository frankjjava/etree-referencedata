<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>2.3.0.RELEASE</version>
		<relativePath />
	</parent>

	<groupId>com.etree</groupId>
	<artifactId>etree-referencedata</artifactId>
	<version>1.0</version>
	<packaging>pom</packaging>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<etree-commons.version>1.0</etree-commons.version>
		<etree-referencedata.version>1.0</etree-referencedata.version>
		<json-org.version>20200518</json-org.version>
		<junit.version>4.13.1</junit.version>
		<cxf.version>3.1.6</cxf.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
			    <groupId>junit</groupId>
			    <artifactId>junit</artifactId>
			    <version>${junit.version}</version>
			    <scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.json</groupId>
				<artifactId>json</artifactId>
				<version>${json-org.version}</version>
			</dependency>

			<dependency>
				<groupId>com.etree</groupId>
				<artifactId>etree-commons-core</artifactId>
				<version>${etree-commons.version}</version>
				<exclusions>
					<exclusion>
						<groupId>com.sun.xml.bind</groupId>
						<artifactId>jaxb-impl</artifactId>
					</exclusion>

					<exclusion>
						<groupId>commons-configuration</groupId>
						<artifactId>commons-configuration</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.apache.commons</groupId>
						<artifactId>commons-lang3</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.cxf</groupId>
				<artifactId>cxf-core</artifactId>
				<version>${cxf.version}</version>
			</dependency>
			<dependency>
				<groupId>com.etree</groupId>
				<artifactId>etree-commons-dao</artifactId>
				<version>${etree-commons.version}</version>
			</dependency>
			<dependency>
				<groupId>com.etree</groupId>
				<artifactId>etree-referencedata-common</artifactId>
				<version>${etree-referencedata.version}</version>
			</dependency>

		</dependencies>
	</dependencyManagement>

	<modules>
		<module>etree-referencedata-common</module>
		<module>etree-referencedata-web</module>
	</modules>
</project>